openapi: 3.0.0
info:
  title: Secure API Gateway(APIGATE)
  version: 1.0.0

paths:
  /v1/echo:
    post:
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
      responses:
        '200':
          description: OK